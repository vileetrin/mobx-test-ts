describe('Autorun', () => {
  it('Should run autorun when observable changed', () => {
    // const CartStore = observable({
    //   const products: string[] = []
    // addProduct(product)
    // });
  });
});